name: CI

on:
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - main
  # Allow this workflow to be called by other workflows (e.g., release)
  workflow_call:

permissions:
  contents: read

jobs:
  # ============================================================================
  # Detect which files changed to optimize job execution
  # ============================================================================
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      docs-only: ${{ steps.filter.outputs.docs-only }}
      code: ${{ steps.filter.outputs.code }}
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Check changed files
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            docs-only:
              - 'docs/**'
              - '*.md'
              - 'CLAUDE.md'
              - 'AGENTS.md'
              - 'README.md'
              - 'CHANGELOG.md'
              - 'LICENSE'
              - '.gitignore'
              - '.editorconfig'
            code:
              - 'src/**'
              - 'tests/**'
              - 'workflows/**'
              - 'spec/**'
              - 'package.json'
              - 'package-lock.json'
              - 'tsconfig*.json'
              - '.github/workflows/**'
              - 'scripts/**'
              - 'console/**'

  # ============================================================================
  # REQUIRED: CI policy guardrails (keeps ruleset-required check stable)
  # ============================================================================
  ci-policy:
    name: CI Policy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js (toolchain)
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies (no lifecycle scripts)
        run: npm install --ignore-scripts

      - name: Run CI policy check
        run: node scripts/ci-policy-check.js

  # ============================================================================
  # REQUIRED: Lockfile is stable under npm install
  # ============================================================================
  lockfile:
    name: Lockfile Stable
    runs-on: ubuntu-latest
    # Advisory only -- lockfile drift should not block PRs. The check still runs
    # and uploads a regenerated lockfile artifact when drift is detected.
    continue-on-error: true

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js (toolchain)
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Regenerate lockfile (no lifecycle scripts)
        run: npm install --ignore-scripts

      - name: Verify lockfile unchanged
        run: git diff --exit-code package-lock.json

      - name: Upload regenerated lockfile (download and commit if this job failed)
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: package-lock-regenerated
          path: package-lock.json

  # ============================================================================
  # REQUIRED (PR only): semantic-release dry-run (config + version validation)
  # ============================================================================
  semantic-release-dry-run:
    name: semantic-release dry-run
    runs-on: ubuntu-latest
    needs: [ changes, lockfile, build-artifact ]
    if: github.event_name == 'pull_request' && needs.changes.outputs.code == 'true'
    permissions:
      contents: read

    steps:
      - name: Checkout code (full history)
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Setup Node.js (toolchain)
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies (no lifecycle scripts)
        run: npm install --ignore-scripts

      - name: semantic-release dry-run (PR context)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release --dry-run --no-ci --verify-conditions false

  # ============================================================================
  # REQUIRED: Build Artifact (single source of truth for dist/)
  # ============================================================================
  build-artifact:
    name: Build Artifact (dist/)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js (toolchain)
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies (no lifecycle scripts)
        run: npm install --ignore-scripts

      - name: Build dist/
        run: npm run build

      # Validates the publishable tarball would build correctly -- catches broken prepack,
      # missing dist/ files, and package.json files field errors. No npm credentials needed.
      - name: Verify release would succeed
        run: npm pack --dry-run

      - name: Verify dist artifacts
        run: |
          test -f dist/mcp-server.js || (echo "Missing dist/mcp-server.js" && exit 1)
          test -f dist/cli-workrail.js || (echo "Missing dist/cli-workrail.js" && exit 1)
          test -f dist/cli.js || (echo "Missing dist/cli.js" && exit 1)
          test -x dist/mcp-server.js || (echo "dist/mcp-server.js is not executable -- bin symlink will fail" && exit 1)
          test -x dist/cli-workrail.js || (echo "dist/cli-workrail.js is not executable -- workrail bin symlink will fail" && exit 1)

      - name: Verify packed tarball has executable bit
        run: |
          npm pack --quiet 2>/dev/null
          tarball=$(ls *.tgz | head -1)
          mode=$(tar -tvzf "$tarball" package/dist/mcp-server.js | awk '{print $1}')
          echo "dist/mcp-server.js packed permissions: $mode"
          mode2=$(tar -tvzf "$tarball" package/dist/cli-workrail.js | awk '{print $1}')
          echo "dist/cli-workrail.js packed permissions: $mode2"
          rm -f "$tarball"
          [[ "${mode:3:1}" == "x" ]] || (echo "dist/mcp-server.js is not executable in published tarball -- prepack script may have failed" && exit 1)
          [[ "${mode2:3:1}" == "x" ]] || (echo "dist/cli-workrail.js is not executable in published tarball -- prepack script may have failed" && exit 1)

      - name: Write dist manifest
        run: node scripts/dist-manifest.js --dist dist --write dist/manifest.json

      - name: Upload dist artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: dist
          path: dist/
          if-no-files-found: error

  # ============================================================================
  # REQUIRED: Type Check (catches type errors that build might miss)
  # ============================================================================
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Type check
        run: npm run typecheck

  # ============================================================================
  # REQUIRED: Security Audit (blocks merge on high/critical vulnerabilities)
  # ============================================================================
  security:
    name: Security Audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Run security audit
        run: |
          echo "## Security Audit Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          npm audit --audit-level=high 2>&1 | tee audit-results.txt
          if grep -q "found 0 vulnerabilities" audit-results.txt; then
            echo "No high/critical vulnerabilities found." >> $GITHUB_STEP_SUMMARY
          else
            echo "High or critical vulnerabilities detected -- merge blocked." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Generate SBOM
        run: |
          echo "## SBOM" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          npm sbom --sbom-format=cyclonedx > sbom.json
          COMPONENT_COUNT=$(node -p "require('./sbom.json').components.length")
          echo "Generated CycloneDX SBOM with ${COMPONENT_COUNT} components (see workflow artifacts)." >> $GITHUB_STEP_SUMMARY

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sbom
          path: sbom.json
          retention-days: 90

  # ============================================================================
  # REQUIRED: Generated artifacts are up to date
  # ============================================================================
  verify-generated:
    name: Verify Generated Artifacts
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies (no lifecycle scripts)
        run: npm install --ignore-scripts

      - name: Verify generated artifacts are up to date
        run: npm run verify:generated

  # ============================================================================
  # REQUIRED: Workflow Validation (blocks merge if workflows are invalid)
  # ============================================================================
  validate-workflows:
    name: Validate Workflows
    runs-on: ubuntu-latest
    needs: [ changes, build-artifact ]
    if: needs.changes.outputs.code == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Download build artifact (dist/)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dist
          path: dist

      - name: Verify dist manifest
        run: node scripts/dist-manifest.js --dist dist --verify dist/manifest.json

      - name: Restore bin execute bit (upload-artifact strips file permissions)
        run: node -e "require('fs').chmodSync('dist/mcp-server.js',0o755)"

      - name: Validate all workflows
        run: npm run validate:registry

      - name: Validate workflow discovery (workflow_list/workflow_get)
        run: npm run validate:workflow-discovery

      - name: Validate authoring spec (structure and sourceRefs)
        run: npm run validate:authoring-spec

      - name: Validate authoring docs (generated docs match spec)
        run: npm run validate:authoring-docs

      - name: Validate feature coverage (all wr.features.* documented in authoring-spec)
        run: npm run validate:feature-coverage

      - name: Check authoring spec staleness (advisory)
        # Informational only -- warns when .ts/.js sourceRef files are newer than lastReviewed.
        # Not a hard gate; set WORKRAIL_STRICT_STALENESS=1 to make it fail.
        continue-on-error: true
        run: npm run validate:authoring-staleness

      - name: Validate authoring spec coverage (required scopes have active rules)
        run: npm run validate:authoring-coverage

      - name: Summary
        if: success()
        run: |
          echo "## Workflow Validation Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All workflow JSON files passed schema validation." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Validated Workflows:" >> $GITHUB_STEP_SUMMARY
          for f in workflows/*.json; do
            echo "- $(basename $f)" >> $GITHUB_STEP_SUMMARY
          done

  # ============================================================================
  # REQUIRED: Build & Test
  # ============================================================================
  build-and-test:
    name: Build & Test (${{ matrix.os }}, Node ${{ matrix.node-version }})
    runs-on: ${{ matrix.os }}
    needs: [ changes, build-artifact ]
    if: needs.changes.outputs.code == 'true'
    strategy:
      matrix:
        # Full matrix on all events: failures surface on PRs, not after merge.
        # This ensures main stays green and releases aren't blocked by post-merge surprises.
        os: [ "ubuntu-latest", "macos-latest", "windows-latest" ]
        node-version: [ "20", "22.14.0" ]
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Download build artifact (dist/)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dist
          path: dist

      - name: Verify dist manifest
        run: node scripts/dist-manifest.js --dist dist --verify dist/manifest.json

      - name: Restore bin execute bit (upload-artifact strips file permissions)
        run: node -e "require('fs').chmodSync('dist/mcp-server.js',0o755)"

      - name: Cross-platform test guardrails
        run: npm run -s codemod:test-platform-guard

      - name: Run unit and integration tests
        run: npx vitest run --exclude="tests/contract/**"

      - name: Upload coverage reports
        if: matrix.node-version == 20
        uses: codecov/codecov-action@b9fd7d16f6d7d1b5d2bec1a2887e65ceed900238 # v4
        with:
          directory: ./coverage
          flags: unittests
          fail_ci_if_error: false
        continue-on-error: true

  # ============================================================================
  # REQUIRED: Contract Tests (JSON-RPC API contract validation)
  # ============================================================================
  contract-tests:
    name: Contract Tests
    runs-on: ubuntu-latest
    needs: [ changes, build-artifact ]
    if: needs.changes.outputs.code == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Download build artifact (dist/)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dist
          path: dist

      - name: Verify dist manifest
        run: node scripts/dist-manifest.js --dist dist --verify dist/manifest.json

      - name: Restore bin execute bit (upload-artifact strips file permissions)
        run: node -e "require('fs').chmodSync('dist/mcp-server.js',0o755)"

      - name: Cross-platform test guardrails
        run: npm run -s codemod:test-platform-guard

      - name: Run contract tests
        run: npx vitest run tests/contract/ --testTimeout=30000
        timeout-minutes: 10

  # ============================================================================
  # REQUIRED: E2E Tests
  # ============================================================================
  e2e-tests:
    name: E2E Tests (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    needs: [ changes, build-artifact ]
    if: needs.changes.outputs.code == 'true'
    concurrency:
      group: workrail-e2e-fixed-3456-${{ github.ref }}-${{ matrix.node-version }}
      cancel-in-progress: false
    strategy:
      matrix:
        # Slim matrix on PRs (Node 22 only), full matrix on main
        node-version: ${{ github.event_name == 'pull_request' && fromJSON('["22.14.0"]') || fromJSON('["20", "22.14.0"]') }}
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Download build artifact (dist/)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dist
          path: dist

      - name: Verify dist manifest
        run: node scripts/dist-manifest.js --dist dist --verify dist/manifest.json

      - name: Restore bin execute bit (upload-artifact strips file permissions)
        run: node -e "require('fs').chmodSync('dist/mcp-server.js',0o755)"

      - name: Install Playwright browsers with dependencies
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests (Chromium only in CI)
        run: npx playwright test --project=chromium --workers=1

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: playwright-report-node-${{ matrix.node-version }}
          path: ./playwright-report/
          retention-days: 30

  # ============================================================================
  # GATE: All required checks must pass
  # This job name must match the required_status_checks in the GitHub ruleset
  # ============================================================================
  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [ changes, ci-policy, lockfile, build-artifact, semantic-release-dry-run, typecheck, verify-generated, validate-workflows, build-and-test, contract-tests, e2e-tests, security ]
    if: always()
    steps:
      - name: Check required jobs
        run: |
          echo "## CI Success Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Lockfile: advisory only (continue-on-error -- does not block merge)
          if [[ "${{ needs.lockfile.result }}" == "success" ]]; then
            echo "- Lockfile Stable: PASSED" >> $GITHUB_STEP_SUMMARY
          else
            echo "- Lockfile Stable: ADVISORY (drift detected -- see artifact)" >> $GITHUB_STEP_SUMMARY
          fi

          # Check build-artifact
          if [[ "${{ needs.build-artifact.result }}" != "success" ]]; then
            echo "- Build Artifact: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Build Artifact job failed"
            exit 1
          fi
          echo "- Build Artifact: PASSED" >> $GITHUB_STEP_SUMMARY

          # Check semantic-release-dry-run (on PRs only, skipped for docs-only)
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            result="${{ needs.semantic-release-dry-run.result }}"
            if [[ "$result" != "success" && "$result" != "skipped" ]]; then
              echo "- semantic-release dry-run: FAILED" >> $GITHUB_STEP_SUMMARY
              echo "semantic-release dry-run job failed"
              exit 1
            fi
            echo "- semantic-release dry-run: PASSED" >> $GITHUB_STEP_SUMMARY
          fi

          # Check typecheck
          if [[ "${{ needs.typecheck.result }}" != "success" ]]; then
            echo "- Type Check: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Type check failed"
            exit 1
          fi
          echo "- Type Check: PASSED" >> $GITHUB_STEP_SUMMARY

          # Check verify-generated
          if [[ "${{ needs.verify-generated.result }}" != "success" ]]; then
            echo "- Verify Generated Artifacts: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Generated artifacts verification failed"
            exit 1
          fi
          echo "- Verify Generated Artifacts: PASSED" >> $GITHUB_STEP_SUMMARY

          # Check validate-workflows (skipped for docs-only)
          result="${{ needs.validate-workflows.result }}"
          if [[ "$result" != "success" && "$result" != "skipped" ]]; then
            echo "- Validate Workflows: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Workflow validation failed"
            exit 1
          fi
          echo "- Validate Workflows: PASSED" >> $GITHUB_STEP_SUMMARY

          # Check build-and-test (skipped for docs-only)
          result="${{ needs.build-and-test.result }}"
          if [[ "$result" != "success" && "$result" != "skipped" ]]; then
            echo "- Build & Test: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Build & Test job failed"
            exit 1
          fi
          echo "- Build & Test: PASSED" >> $GITHUB_STEP_SUMMARY

          # Check contract-tests (skipped for docs-only)
          result="${{ needs.contract-tests.result }}"
          if [[ "$result" != "success" && "$result" != "skipped" ]]; then
            echo "- Contract Tests: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Contract Tests job failed"
            exit 1
          fi
          echo "- Contract Tests: PASSED" >> $GITHUB_STEP_SUMMARY

          # Check e2e-tests (skipped for docs-only)
          result="${{ needs.e2e-tests.result }}"
          if [[ "$result" != "success" && "$result" != "skipped" ]]; then
            echo "- E2E Tests: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "E2E Tests job failed"
            exit 1
          fi
          echo "- E2E Tests: PASSED" >> $GITHUB_STEP_SUMMARY

          # Check security audit
          if [[ "${{ needs.security.result }}" != "success" ]]; then
            echo "- Security Audit: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Security audit found high/critical vulnerabilities"
            exit 1
          fi
          echo "- Security Audit: PASSED" >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All required CI checks passed." >> $GITHUB_STEP_SUMMARY
