name: Release (dry-run)

on:
  workflow_dispatch:

permissions:
  # semantic-release dry-run verifies it can push tags/commits; requires write access.
  contents: write

jobs:
  dry-run:
    name: semantic-release dry-run
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code (full history)
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Setup Node.js (toolchain)
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22.14.0
          cache: 'npm'

      # corepack reads the packageManager field from package.json and activates
      # the correct npm version, reliably shadowing the bundled npm in PATH.
      # (Kept in sync with the real release job even though this fork no
      # longer publishes to a registry, so `npm ci`/`npm install` behavior
      # matches exactly between dry-run and real releases.)
      - name: Activate npm (corepack reads packageManager from package.json)
        run: corepack enable npm && corepack prepare --activate

      - name: Install dependencies (no lifecycle scripts)
        run: npm install --ignore-scripts

      - name: semantic-release dry-run
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WORKRAIL_ALLOW_MAJOR_RELEASE: ${{ vars.WORKRAIL_ALLOW_MAJOR_RELEASE }}
        run: |
          set -euo pipefail

          # Mirror the real release job: use the corepack-activated npm and avoid
          # PATH shadowing from ./node_modules/.bin when invoking semantic-release.
          export PATH="$(echo "$PATH" | tr ':' '\n' | awk '!/\/node_modules\/\.bin/' | paste -sd ':' -)"
          hash -r

          node ./node_modules/semantic-release/bin/semantic-release.js --dry-run --no-ci
