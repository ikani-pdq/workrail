{
  "id": "wr.mr-review",
  "name": "MR Review Workflow",
  "version": "2.12.0",
  "description": "Evidence-driven MR review with intent-first gating, caller enumeration for transitive regression detection, per-family checklists with finding caps, causal attribution enforcement, hidden reflection scoring, and a typed verdict schema with file/line anchors.",
  "about": "## MR Review Workflow\n\nThis workflow conducts a structured, evidence-driven code review of a merge request or pull request. It is designed for cases where you want a thorough, audit-quality review rather than a quick glance -- particularly when the change touches critical surfaces, spans many files, or carries real production risk.\n\n**What it does:**\nThe workflow locates and bounds the review target, enumerates callers of changed functions to detect transitive regressions, checks intent alignment before correctness, freezes a neutral fact packet, runs parallel reviewer families each with domain-specific checklists and a finding cap, applies hidden reflection scoring to suppress low-confidence findings, reconciles contradictions, stress-tests the recommendation adversarially, and produces a final handoff with severity-graded, file/line-anchored findings and a typed verdict.\n\n**When to use it:**\n- Before merging a PR that touches auth, data models, APIs, or critical paths\n- When you want independent perspectives on a change without the noise of an unstructured review\n- When the change is large or the reviewer is unfamiliar with the surrounding code\n- When you need a reproducible audit trail for compliance or team review processes\n\n**What it produces:**\nA final review recommendation (approve / request changes / needs discussion) with a confidence band, severity-graded findings anchored to specific files and lines, ready-to-post MR comments, a coverage ledger, and a typed `wr.review_verdict` artifact.\n\n**How to get good results:**\nProvide the PR URL, branch name, or diff. If the change has non-obvious intent, a one-sentence description of the goal helps. The workflow will not post comments or approve/reject without explicit instruction.",
  "examples": [
    "Review PR #47 adding JWT refresh token rotation before it merges to main",
    "Review the feature/cache-expiration branch for correctness and production risk before merging",
    "Audit PR #312 refactoring the payment service data model for backward compatibility and rollout safety",
    "Review the open MR for the new rate-limiting middleware against our API contract standards"
  ],
  "recommendedPreferences": {
    "recommendedAutonomy": "guided",
    "recommendedRiskPolicy": "conservative"
  },
  "metricsProfile": "review",
  "validatedAgainstSpecVersion": 3,
  "features": [
    "wr.features.subagent_guidance"
  ],
  "assessments": [
    {
      "id": "evidence-quality-gate",
      "purpose": "Key findings are backed by specific code references, line numbers, or concrete observations -- not intuition or pattern-matching alone.",
      "dimensions": [
        {
          "id": "evidence_quality",
          "purpose": "Each finding cites a specific file, function, or line. No finding relies on intuition or pattern-matching without concrete grounding.",
          "levels": [
            "low",
            "high"
          ]
        }
      ]
    },
    {
      "id": "coverage-completeness-gate",
      "purpose": "All relevant review domains have been adequately checked for this change. No material blind spots remain unacknowledged.",
      "dimensions": [
        {
          "id": "coverage_completeness",
          "purpose": "All material review domains are checked or explicitly acknowledged as gaps in the coverage ledger.",
          "levels": [
            "low",
            "high"
          ]
        }
      ]
    },
    {
      "id": "contradiction-resolution-gate",
      "purpose": "Material contradictions and competing interpretations are resolved or explicitly acknowledged with a clear rationale for the chosen position.",
      "dimensions": [
        {
          "id": "contradiction_resolution",
          "purpose": "Every material contradiction is resolved by evidence or explicitly acknowledged with a stated position and rationale.",
          "levels": [
            "low",
            "high"
          ]
        }
      ]
    },
    {
      "id": "finding-schema-compliance-gate",
      "purpose": "Every Critical or Major finding in the final output has a file, line reference, causal link to the PR changes, and a concrete remediation step -- not just a flag.",
      "dimensions": [
        {
          "id": "finding_schema_compliance",
          "purpose": "Every Critical/Major finding has: specific file+line, causal link explaining why this PR introduced or worsened it, and a concrete remediation or verification step. Findings without all three are flags, not actionable review comments.",
          "levels": [
            "low",
            "high"
          ]
        }
      ]
    }
  ],
  "preconditions": [
    "User provides an MR/PR objective, diff, patch, branch, or equivalent review target.",
    "The agent can inspect the changed files, surrounding code, and any deterministic validation artifacts needed to review accurately.",
    "A human reviewer or author will consume the final recommendation and findings."
  ],
  "metaGuidance": [
    "DEFAULT BEHAVIOR: self-execute with tools. Only ask for missing external artifacts, permissions, or business context you cannot resolve yourself.",
    "V2 DURABILITY: use output.notesMarkdown and explicit `continue_workflow` context keys as durable workflow state. Do NOT rely on the review document as required workflow memory.",
    "ARTIFACT STRATEGY: `reviewDocPath` is an optional human-facing artifact only. Create or update it only when it materially improves handoff or readability. Workflow truth lives in notes and explicit context fields.",
    "NOTES QUALITY: notes should work for both a human reader and a future agent resuming later. For important phases, make clear what was learned, what was decided, what remains uncertain, and what should happen next.",
    "OWNERSHIP & DELEGATION: the main agent owns truth, synthesis, severity calibration, recommendation, and final handoff. Delegate only bounded reviewer or validation work through the WorkRail Executor.",
    "SUBAGENT SYNTHESIS: treat reviewer-family and validator output as evidence, not conclusions. State your current hypothesis before delegation, then say what was confirmed, what was new, what you reject, and what changed your mind.",
    "PARALLELISM: parallelize independent cognition; serialize canonical synthesis, coverage-ledger updates, recommendation decisions, and document writes.",
    "REVIEW MODEL: reconstruct intent first, enumerate transitive impact, freeze a neutral fact packet, then let reviewer families challenge it in parallel, then reconcile contradictions explicitly.",
    "COVERAGE LEDGER: explicitly track review domains as `checked`, `uncertain`, `not_applicable`, `contradicted`, or `needs_followup`. Do not finalize with unresolved material gaps unless you name them clearly.",
    "TRIGGERS: WorkRail can only react to explicit fields. Use structural fields such as `contextUnknownCount`, `criticalSurfaceTouched`, `coverageUncertainCount`, `contradictionCount`, `falsePositiveRiskCount`, `blindSpotCount`, and `needsSimulation`.",
    "FINDING CONTRACT: a finding requires all of: specific file+line, affected code path, concrete failure mode, why this PR introduced or worsened it, and a credible remediation step. Missing any of these: convert to a clarification question or drop it.",
    "FINDING CAP: QUICK/STANDARD = max 3 findings per reviewer family; THOROUGH = max 5. Rank by severity x confidence x actionability. Extras are noise -- drop them.",
    "CLARIFICATION PATHWAY: when you do not have enough context to make a confident finding, emit a clarification question instead of a low-confidence finding. 'Clarification needed' is a valid and valuable output state.",
    "CAUSAL ATTRIBUTION: for every finding, state whether it could have pre-existed. Only flag issues where you can articulate a causal connection to the changed lines or their direct dependents. Do not flag pre-existing issues as regressions.",
    "HIDDEN REFLECTION: after Phase 3, score each finding 0-10 for confidence x actionability and suppress findings scoring below 4. This is an internal quality pass -- never surface scores or suppression rationale to reviewers.",
    "BOUNDARY DISCIPLINE: attempt to determine the real review target and the likely ancestor-relative review surface. If that confidence remains weak, continue with downgraded confidence and disclose the limitation clearly instead of pretending certainty.",
    "SOURCE DISCOVERY: opportunistically recover PR context, ticket/docs context, and repo/user policy context from the strongest available sources. Missing sources should usually lower confidence and be disclosed, not block the workflow.",
    "BOUNDARY: do not post comments, approve, reject, or merge unless the user explicitly asks. Produce findings, recommendation, and handoff material only."
  ],
  "steps": [
    {
      "id": "phase-0-understand-and-classify",
      "title": "Phase 0: Locate, Bound, Enrich, Classify & Enumerate Callers",
      "prompt": "Build the review foundation in one pass, including transitive impact discovery.\n\n**Step 1 -- Early exit / minimum inputs:**\nBefore exploring, verify that the review target is real and inspectable. If the diff, changed files, or equivalent review material are completely absent and cannot be inferred with tools, ask for the minimum missing artifact and stop. Do NOT ask questions you can resolve with tools.\n\n**Step 2 -- Locate and bound the review target:**\nAttempt to determine the strongest available review target and boundary.\n\nAttempt to establish:\n- `reviewTargetKind` from the strongest available source (PR/MR, branch, patch, diff, or local working tree changes)\n- `reviewTargetSource` describing where the target came from\n- likely PR/MR identity when available (`prUrl`, `prNumber`)\n- likely base / ancestor reference (`baseCandidate`, `mergeBaseRef`) when available\n- whether the branch may include inherited or out-of-scope changes\n- `boundaryConfidence`: High / Medium / Low\n\nDo not over-prescribe your own investigation path. Use the strongest available evidence and record uncertainty honestly.\n\n**Step 3 -- Enrich with context:**\nRecover the strongest available intent and policy context.\n\nAttempt to recover:\n- MR title, purpose, and PR description\n- ticket / issue / acceptance context (`ticketRefs`, `ticketContext`)\n- supporting docs / specs / rollout context (`supportingDocsFound`)\n- repo or user policy/convention context (`policySourcesFound`) -- check AGENTS.md, CLAUDE.md, .workrail/review-rules.md, or equivalent\n- `contextConfidence`: High / Medium / Low\n\n**Step 4 -- Review-surface hygiene:**\nClassify the visible change into a minimal review surface.\n\nSet:\n- `coreReviewSurface`\n- `likelyNoiseOrMechanicalChurn`\n- `likelyInheritedOrOutOfScopeChanges`\n- `reviewSurfaceSummary`\n- `reviewScopeWarnings`\n\n**Step 5 -- Classify the review:**\nAfter exploration, classify the work.\n\nSet:\n- `reviewMode`: QUICK / STANDARD / THOROUGH\n- `riskLevel`: Low / Medium / High\n- `shapeProfile`: `isolated_change`, `crosscutting_change`, `mechanically_noisy_change`, or `ambiguous_boundary`\n- `changeTypeProfile`: `general_code_change`, `api_contract_change`, `data_model_or_migration`, `security_sensitive`, or `test_only`\n- `maxParallelism`: 0 / 3 / 5\n- `criticalSurfaceTouched`: true / false\n- `needsSimulation`: true / false\n- `needsBoundaryFollowup`: true / false\n- `needsContextFollowup`: true / false\n- `needsReviewerBundle`: true / false\n\nDecision guidance:\n- QUICK: very small, isolated, low-risk changes with little ambiguity\n- STANDARD: typical feature or bug-fix reviews with moderate ambiguity or moderate risk\n- THOROUGH: critical surfaces, architectural novelty, high risk, broad change sets, or strong need for independent reviewer perspectives\n\nRouting guidance:\n- `api_contract_change` -> bias toward contract/consumer/backward-compatibility scrutiny\n- `data_model_or_migration` -> bias toward rollout/compatibility/simulation scrutiny\n- `security_sensitive` -> bias toward adversarial/runtime-risk scrutiny, lower tolerance for weak evidence\n- `test_only` -> bias toward stronger false-positive suppression\n- `mechanically_noisy_change` -> bias toward stronger noise filtering\n\n**Step 6 -- Caller enumeration (transitive impact discovery):**\nThis step discovers regressions that diff-anchoring alone cannot catch.\n\nFor each changed function, method, or exported symbol in the diff:\n1. Extract the name from the changed lines\n2. Run `git grep -n \"<name>\"` (or ripgrep) across the repository, excluding the changed files themselves\n3. Collect results: unchanged files that call, import, or reference the changed symbol\n4. For each caller found, note: file path, line number, and how it uses the symbol\n5. Assess whether the change's contract (signature, return type, behavior) could affect each caller\n\nSet:\n- `callerEnumerationResults`: array of `{ changedSymbol, callers: [{ file, line, usage }], contractChangeRisk: 'high'|'medium'|'low'|'none' }`\n- `callerEnumerationCount`: total number of external callers found\n- `callerEnumerationHighRiskCount`: callers where contractChangeRisk is 'high'\n- `callerEnumerationConfidence`: High / Medium / Low\n\nFallback: if grep/ripgrep is unavailable or the codebase is too large to enumerate, set `callerEnumerationConfidence: Low` and record the limitation. Do not block the review.\n\nHigh-risk callers feed directly into the `correctness_invariants` reviewer family in Phase 3.\n\n**Step 7 -- Optional deeper context:**\nIf `reviewMode` is STANDARD or THOROUGH and context remains incomplete, spawn TWO WorkRail Executors SIMULTANEOUSLY running `routine-context-gathering` with agentConfig={ modelTier: 'lightweight' }, focus=COMPLETENESS and focus=DEPTH. Synthesize both outputs before finishing this step.\n\n**Step 8 -- Human-facing artifact:**\nChoose `reviewDocPath` only if a live artifact will materially improve human readability. Default: `mr-review.md` at project root. Optional, never canonical workflow state.\n\nFallback behavior:\n- PR/MR not found but branch/diff is inspectable -> continue with downgraded context confidence\n- merge-base / ancestor ambiguous -> continue, set `needsBoundaryFollowup = true`, disclose\n- ticket or supporting docs missing -> continue with downgraded context confidence\n- only a patch/diff available -> continue if inspectable, lower confidence on intent-dependent conclusions\n- review target itself missing -> ask only for that missing artifact and stop\n\nSet these context keys:\n`reviewTargetKind`, `reviewTargetSource`, `prUrl`, `prNumber`, `baseCandidate`, `mergeBaseRef`, `boundaryConfidence`, `contextConfidence`, `mrTitle`, `mrPurpose`, `ticketRefs`, `ticketContext`, `supportingDocsFound`, `policySourcesFound`, `accessibleContextSources`, `missingContextSources`, `focusAreas`, `changedFileCount`, `criticalSurfaceTouched`, `reviewMode`, `riskLevel`, `shapeProfile`, `changeTypeProfile`, `maxParallelism`, `reviewDocPath`, `contextSummary`, `candidateFiles`, `moduleRoots`, `contextUnknownCount`, `coverageGapCount`, `authorIntentUnclear`, `needsSimulation`, `needsBoundaryFollowup`, `needsContextFollowup`, `needsReviewerBundle`, `coreReviewSurface`, `likelyNoiseOrMechanicalChurn`, `likelyInheritedOrOutOfScopeChanges`, `reviewSurfaceSummary`, `reviewScopeWarnings`, `openQuestions`, `callerEnumerationResults`, `callerEnumerationCount`, `callerEnumerationHighRiskCount`, `callerEnumerationConfidence`\n\nAlso set a one-sentence goal in context (e.g. 'review PR #47 before merge') to populate the session title.\n\nRules:\n- answer your own questions with tools whenever possible\n- only keep true human-decision questions in `openQuestions`\n- classify AFTER exploring, not before\n- caller enumeration runs even for QUICK mode -- it is cheap and catches the most important regression class",
      "requireConfirmation": {
        "or": [
          {
            "var": "reviewMode",
            "equals": "THOROUGH"
          },
          {
            "var": "riskLevel",
            "equals": "High"
          }
        ]
      }
    },
    {
      "id": "phase-0b-intent-scope-and-completeness-gate",
      "title": "Phase 0b: Intent, Scope & Completeness Gate",
      "prompt": "Verify intent alignment before correctness review. If the PR does not do what was asked, deeper review of whether it does it correctly is wasted work.\n\n**Step 1 -- Intent alignment check (runs first, gates the rest):**\nFrom the ticket/issue/PR description recovered in Phase 0, reconstruct the author's intent:\n- What problem was this PR supposed to solve?\n- What behavior change was expected?\n- Is the diff consistent with that intent, or does it solve a different or adjacent problem?\n\nClassify intent alignment:\n- `aligned`: the diff clearly implements what the ticket/description asked for\n- `partial`: the diff addresses some of the stated intent but appears to miss part of it\n- `misaligned`: the diff solves a different problem than what was described\n- `unclear`: insufficient ticket/description context to assess -- set `authorIntentUnclear = true`\n\nIf `misaligned`: this is a blocking finding regardless of code correctness. Record it as `intentMisalignmentFinding` and set `intentAlignmentStatus = misaligned`. The remaining steps in this phase still run, but the reviewer families in Phase 3 should treat intent misalignment as the primary finding.\n\nIf `acceptanceCriteriaSource = none` and `authorIntentUnclear = true`: set `scopeCheckConfidence = Low` and continue with downgraded confidence.\n\n**Step 2 -- Enumerate acceptance criteria:**\nFrom the ticket/issue/PR description, extract a flat list of acceptance criteria. Mark each as `explicit` (stated in ticket/issue) or `inferred` (derived from goal).\n\nFor each criterion, examine the diff and determine:\n- `met`: the diff clearly addresses this criterion (cite specific files or functions)\n- `partial`: the diff partially addresses it but something appears missing\n- `missing`: the diff does not appear to address this criterion at all\n- `unclear`: insufficient context to judge\n\n**Step 3 -- Scope creep check:**\nLook for changes in the diff that go beyond what any acceptance criterion requires:\n- behavior modifications not mentioned in the ticket/goal\n- files unrelated to the stated purpose\n- new abstractions or refactors not required by the task\n\nDistinguish necessary implementation details from genuine scope creep.\n\n**Step 4 -- Set context keys:**\n- `intentAlignmentStatus`: `aligned` | `partial` | `misaligned` | `unclear`\n- `intentMisalignmentFinding`: description of misalignment if status is `misaligned` (empty string otherwise)\n- `acceptanceCriteria`: array of `{ criterion, source: 'explicit'|'inferred', status: 'met'|'partial'|'missing'|'unclear', evidence? }`\n- `acceptanceCriteriaSource`: `'ticket'` | `'pr_description'` | `'inferred'` | `'none'`\n- `missingCriteriaCount`: number of criteria with status `missing` or `partial`\n- `scopeCreepFlags`: array of specific out-of-scope changes found (empty array if none)\n- `scopeCreepCount`: length of `scopeCreepFlags`\n- `scopeCheckConfidence`: `High` | `Medium` | `Low`\n\nRules:\n- intent check runs first and its result informs how seriously to take subsequent correctness findings\n- do not block the review on unclear criteria -- record uncertainty and continue\n- a criterion is only `missing` if you can confirm the behavior is absent from the diff, not just from a single file\n- scope creep findings feed into `patterns_architecture` or `philosophy_alignment` reviewers -- do not duplicate them",
      "requireConfirmation": false
    },
    {
      "id": "phase-1-state-hypothesis",
      "title": "Phase 1: State Review Hypothesis",
      "prompt": "Before selecting reviewer families, state your current review hypothesis in 3-5 sentences.\n\nBased on what you learned in Phases 0 and 0b, write:\n1. your current best guess about the review recommendation direction\n2. the issue category or risk you are most concerned about -- correctness, intent misalignment, transitive caller impact, security, architecture, or something else\n3. what would most likely make that current view wrong\n\nIf `callerEnumerationHighRiskCount > 0`, explicitly address whether transitive caller breakage could change your recommendation.\n\nIf `intentAlignmentStatus` is not `aligned`, note whether that alone makes the PR blocking regardless of correctness.\n\nThis is your reference point. After reviewer-family and validation work, you will compare the result against this hypothesis and say what changed your mind or what held firm.\n\nSet this key in the next `continue_workflow` call's `context` object:\n- `recommendationHypothesis`",
      "requireConfirmation": false
    },
    {
      "id": "phase-2-freeze-fact-packet-and-select-reviewers",
      "title": "Phase 2: Freeze Fact Packet and Select Reviewer Families",
      "promptBlocks": {
        "goal": "Freeze a shared factual basis for review and decide how much reviewer-family parallelism is warranted.",
        "constraints": [
          [
            {
              "kind": "ref",
              "refId": "wr.refs.notes_first_durability"
            }
          ],
          "The fact packet is the primary truth for downstream reviewer families.",
          "Keep `recommendationHypothesis` as a secondary hypothesis to challenge, not a frame to defend.",
          "Include the full bodies of changed functions with [NEW]/[UNCHANGED] line markers when the PR is not large (fewer than 10 changed functions). For larger PRs, include at minimum the function signature and the changed hunk with 8 lines of context before the change and 2 lines after."
        ],
        "procedure": [
          "Create a neutral `reviewFactPacket` containing: MR purpose and expected behavior change, intent alignment status and any misalignment finding, review target and review-surface summary, changed files and module roots, key contracts / invariants / affected consumers, call-chain highlights, **caller enumeration results** (callerEnumerationResults -- list all high-risk callers explicitly), relevant repo patterns from AGENTS.md/CLAUDE.md/.workrail/review-rules.md, tests/docs expectations, discovered ticket/doc/policy context, accessible and missing context sources, explicit open unknowns, relevant coding philosophy principles (scope to the 3-5 most relevant for what was changed), existing patterns in the changed module, the acceptance criteria list and scope check results from Phase 0b.",
          "Initialize `coverageLedger` for these domains: `correctness_logic`, `contracts_invariants`, `patterns_architecture`, `philosophy_alignment`, `runtime_production_risk`, `tests_docs_rollout`, `security_performance`, `intent_alignment`, `transitive_impact`, `ticket_domain_fit`.",
          "Perform a preliminary self-review from the fact packet before choosing reviewer families.",
          "Reviewer family options: `correctness_invariants`, `patterns_architecture`, `philosophy_alignment`, `runtime_production_risk`, `test_docs_rollout`, `security`, `ticket_compliance`, `ticket_domain_fit`, `false_positive_skeptic`, `missed_issue_hunter`, `prior_review_signals`, `visibility_scope`.",
          "Selection guidance: QUICK = no bundle by default unless ambiguity feels material; STANDARD = 3 families by default; THOROUGH = 5 families by default.",
          "Always include `correctness_invariants`. Include `test_docs_rollout` in STANDARD and THOROUGH unless clearly not applicable. Include `runtime_production_risk` when `criticalSurfaceTouched = true` or `needsSimulation = true`. Include `security` when `changeTypeProfile = security_sensitive` or when the diff touches auth, session, crypto, permissions, or input handling. Include `ticket_compliance` when `acceptanceCriteriaSource != 'none'` and `missingCriteriaCount > 0` or `scopeCreepCount > 0`. Include `ticket_domain_fit` in QUICK, STANDARD, and THOROUGH whenever real ticket context was recovered (`ticketRefs` or `ticketContext` non-empty) and `changeTypeProfile != 'test_only'` -- this is a cheap, text-level cross-check (does the ticket's own stated product area/domain match what the touched code is actually named/documented for), not a deep investigation, so unlike `runtime_production_risk`/`security` it should not be gated behind risk level: a mismatch is exactly as likely to hide in a small, clean-looking diff as a large risky one, and the other families have no mandate to catch it (`ticket_compliance` checks whether acceptance criteria were met, never whether the ticket's domain matches the subsystem touched; `philosophy_alignment`/`correctness_invariants` never read the ticket's own domain language against the code's). Include `missed_issue_hunter` in THOROUGH. Include `false_positive_skeptic` when Major/Critical findings seem plausible or severity inflation risk is non-trivial. Include `philosophy_alignment` in STANDARD and THOROUGH when the change introduces new abstractions, modifies core patterns, or when `scopeCreepCount > 0` or `missingCriteriaCount > 0`. Include `prior_review_signals` when STANDARD or THOROUGH and the PR touches files that have prior review history (most non-trivial PRs qualify); it adds the historical signal the current-diff angles cannot see. Include `visibility_scope` in STANDARD and THOROUGH unconditionally, and in QUICK when `changeTypeProfile = api_contract_change` or when the diff contains any visibility widening (private -> protected, internal -> exported, etc.); it checks whether existing module boundaries are enforced by visibility modifiers, a gap no other family has a mandate to fill.",
          "Set `coverageUncertainCount` as the number of coverage domains not yet safely closed.",
          "Set `selectedReviewerFamilies` in the context as an array of strings representing the selected reviewer families to run (e.g. `[\"correctness_invariants\", \"security\"]`). Options: `correctness_invariants`, `patterns_architecture`, `runtime_production_risk`, `security`, `test_docs_rollout`, `philosophy_alignment`, `false_positive_skeptic`, `ticket_compliance`, `ticket_domain_fit`, `missed_issue_hunter`, `prior_review_signals`, `visibility_scope`.",
          "Initialize `contradictionCount`, `blindSpotCount`, and `falsePositiveRiskCount` to `0` if no reviewer-family bundle will run."
        ],
        "verify": [
          "The fact packet is concrete enough that downstream reviewer families can use it without regathering broad context.",
          "Caller enumeration results are explicitly included in the fact packet.",
          "The workflow has a clear reason for whether `needsReviewerBundle` is true or false."
        ]
      },
      "requireConfirmation": false
    },
    {
      "id": "phase-3-reviewer-family-bundle",
      "title": "Phase 3: Parallel Reviewer Family Bundle",
      "type": "parallel",
      "runCondition": {
        "var": "needsReviewerBundle",
        "equals": true
      },
      "parallelDelegations": [
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "correctness_invariants"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "correctness_invariants",
            "familyMission": "logic errors, invariant violations, API contract breaks, transitive caller impact.",
            "familyChecklist": "(1) Does the changed logic handle all branches including error paths and edge cases? (2) Do the callerEnumerationResults show any caller whose contract assumptions are now violated? (3) Are all invariants that callers depend on still upheld? (4) Are there null/undefined/empty cases the change doesn't handle? (5) Does the change break any existing tests or cause them to test the wrong thing?",
            "deliverableName": "docs/design/reviewer-findings-correctness-invariants.md"
          },
          "modelTier": "mid"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "patterns_architecture"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "patterns_architecture",
            "familyMission": "pattern fit, design consistency, architectural concerns, scope creep.",
            "familyChecklist": "(1) Does the change belong in the layer/module where it was placed? (2) Does it introduce unnecessary complexity or duplicate existing solutions? (3) Does it respect the module's existing abstractions without adding leaky ones? (4) Is scope creep present -- changes beyond what the ticket requires? (5) Would a future maintainer understand why this was placed here? (6) Before raising a cross-team ownership or sign-off concern (the change touches a module/path owned by a team other than the author's own, per CODEOWNERS or an equivalent ownership doc) as an open question to a human reviewer, verify it mechanically first: check the real approval rules for the MR/PR (e.g. `glab api projects/:id/merge_requests/:iid/approval_rules`, the GitHub required-reviewers API, or the platform equivalent) to see whether that team's sign-off is ALREADY a required, structurally-enforced rule on the touched path. If it is, do not ask the author whether that team needs to sign off -- either state it as a confirmed, already-covered fact if worth mentioning, or omit it entirely; never pose an already-mechanically-answerable fact as a question. Only raise cross-team ownership as a genuine open question when the check shows the relevant team's sign-off is NOT already required.",
            "deliverableName": "docs/design/reviewer-findings-patterns-architecture.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "runtime_production_risk"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "runtime_production_risk",
            "familyMission": "runtime behavior, production impact, performance, state-flow, rollout safety.",
            "familyChecklist": "(1) Are there performance implications (N+1, blocking calls, memory retention)? (2) Is the change safe under concurrent access? (3) Does it affect rollout safety (feature flags, migrations, backward compatibility)? (4) Are error cases handled in a way that degrades gracefully? (5) Is the change observable -- are there logs/metrics to detect if it breaks in production?",
            "deliverableName": "docs/design/reviewer-findings-runtime-production-risk.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "security"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "security",
            "familyMission": "adversarial analysis: trust boundaries, attacker-controlled inputs, privilege escalation, injection risks.",
            "familyChecklist": "(1) What inputs in the changed code path are attacker-controlled? Trace each. (2) Does the change cross a trust boundary -- does it pass attacker-controlled data to a privileged operation? (3) Are there injection risks (SQL, command, path traversal, template)? (4) Does the change affect auth, session handling, permissions, or crypto? If yes, what could an attacker do with the change? (5) Are secrets, tokens, or credentials handled correctly?\nNOTE: approach this as an attacker, not a code auditor. Start by identifying what an attacker controls, then ask how the change could be exploited.",
            "deliverableName": "docs/design/reviewer-findings-security.md"
          },
          "modelTier": "mid"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "test_docs_rollout"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "test_docs_rollout",
            "familyMission": "test adequacy, documentation, migration, rollout, affected consumers.",
            "familyChecklist": "(1) Do the new/changed tests actually exercise the new behavior, or do they test implementation details? (2) Are error paths and edge cases tested? (3) Is the public API or behavior change documented? (4) If there is a migration or schema change, is the rollout sequence safe? (5) Are downstream consumers or dependents identified and accounted for?",
            "deliverableName": "docs/design/reviewer-findings-test-docs-rollout.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "philosophy_alignment"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "philosophy_alignment",
            "familyMission": "implementation approach vs. coding philosophy, alternative approaches.",
            "familyChecklist": "(1) For each relevant principle from the fact packet, does the implementation follow it, violate it, or is neutral? Name violations by principle and cite specific code. (2) Generate 2-3 alternative approaches that would also satisfy the acceptance criteria -- for each, state what it looks like in one sentence, which philosophy principles it scores better/worse on, and why it was or was not the right choice here. (3) Is the chosen approach the best one given the philosophy, or merely a correct one? (4) Are there abstractions that would make illegal states unrepresentable that the author missed? (5) Does the change handle errors as data (Result types) rather than exceptions?",
            "deliverableName": "docs/design/reviewer-findings-philosophy-alignment.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "false_positive_skeptic"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "false_positive_skeptic",
            "familyMission": "challenge overreaches, weak evidence, severity inflation.",
            "familyChecklist": "find the strongest case that other families are wrong. For each Major or Critical finding from other families: (1) Is the evidence strong enough to justify the severity? (2) Could this issue have pre-existed before this PR? (3) Is the affected code path actually reachable? (4) Does the suggested fix actually address the root cause? (5) Is the reviewer demonstrating codebase knowledge or pattern-matching on surface appearance.",
            "deliverableName": "docs/design/reviewer-findings-false-positive-skeptic.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "ticket_compliance"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "ticket_compliance",
            "familyMission": "verify the PR delivers what the ticket required.",
            "familyChecklist": "(1) For each acceptance criterion from `acceptanceCriteria` with status `missing` or `partial`, confirm whether it is truly absent from the diff or whether Phase 0b assessment was wrong. (2) For each `scopeCreepFlag`, assess severity: is this a necessary implementation detail or genuine unasked-for scope? (3) Does the PR description accurately describe what the diff does? (4) Are there acceptance criteria that are `met` on paper but implemented in a way that misses the spirit of the requirement? (5) Would the ticket author consider this PR done?",
            "deliverableName": "docs/design/reviewer-findings-ticket-compliance.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "ticket_domain_fit"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "ticket_domain_fit",
            "familyMission": "reconcile the ticket's own stated product area/domain against what the touched code is actually named and documented for -- a cross-check no other family has a mandate to make.",
            "familyChecklist": "(1) Read the ticket/ticketContext's OWN text (not just its title or team field) -- what product area, user-facing feature, or domain scenario does it actually describe? (2) Read the touched code's own domain signals -- class/package names, KDoc/comments, existing domain terms already present in the file(s) -- what subsystem or domain do THEY say this code belongs to? (3) Do the two match? If the ticket describes one domain and the touched code is named or documented for a visibly different one, this is a finding regardless of how clean or small the diff itself looks. (4) Do NOT resolve an apparent mismatch by trusting the author's own inline comment, commit message, or PR description as settling it -- an author's own claim about why cross-domain code is the right place is exactly the kind of claim this check exists to verify independently, never accept at face value. (5) If the ticket itself is not in a resolved/confirmed state (e.g. blocked, pending a product decision, open questions in its own comments/description), do not describe any technical claim as 'confirmed via ticket' -- state plainly that the ticket does not actually confirm it. (6) Conclude explicitly: either 'domain matches, no concern' with one line of reasoning citing what you compared, or a concrete, specific question a human with real product/domain context should answer before this merges -- never stay silent on a real mismatch just because the code change itself is small or mechanically correct.",
            "deliverableName": "docs/design/reviewer-findings-ticket-domain-fit.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "missed_issue_hunter"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "missed_issue_hunter",
            "familyMission": "identify one class of issue the other families are structurally blind to.",
            "familyChecklist": "identify one class of issue the other families are structurally blind to. Consider: (1) Cross-cutting concerns that span multiple changed files (2) Second-order effects of caller contract changes not yet enumerated (3) Security concerns outside the explicit security domain (timing attacks, resource exhaustion, SSRF) (4) Operational concerns (config drift, deployment dependencies, secret rotation) (5) Long-term maintainability issues the current reviewers didn't flag",
            "deliverableName": "docs/design/reviewer-findings-missed-issue-hunter.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "prior_review_signals"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "prior_review_signals",
            "familyMission": "surfaces patterns from review history on the same files to catch recurring issues and apply prior reviewer judgment.",
            "familyChecklist": "(1) Find merged MRs/PRs that touched any of the same files changed in this PR. For GitHub repos use: `gh pr list --state merged --json number,title,files` then `gh pr view <number> --json reviews,comments,reviewThreads` for the top 3-5 most recent. For GitLab repos use the available MCP tools. If neither is accessible, note the gap and skip. (2) Identify any review comments on those prior MRs that flagged issues in the same functions or code paths changed by this PR. (3) For each prior finding: does this PR re-introduce the same pattern, or fail to carry forward the fix that was applied previously? (4) Are there recurring comments (same reviewer flagged the same thing in 2+ prior MRs) that now apply to this PR? These carry extra weight. (5) Did any prior MR introduce a pattern this PR departs from without explanation?",
            "deliverableName": "docs/design/reviewer-findings-prior-review-signals.md"
          },
          "modelTier": "lightweight"
        },
        {
          "workflowId": "wr.routine-reviewer-family",
          "runCondition": {
            "var": "selectedReviewerFamilies",
            "contains": "visibility_scope"
          },
          "contextMapping": {
            "reviewFactPacket": "reviewFactPacket"
          },
          "args": {
            "familyName": "visibility_scope",
            "familyMission": "encapsulation enforcement -- given the module boundaries and abstractions that exist, symbols, types, and members carry the narrowest visibility their actual usage permits; visibility widenings introduced by the PR are justified by architecture, not convenience.",
            "familyChecklist": "(1) Are any symbols (functions, classes, types, constants) exported by this PR that are only referenced within the same module? If yes, they should not be exported. (2) Are any class or object members marked public or protected where private or package-internal would suffice given actual usage in the diff? (3) Does any barrel or index file introduced or modified by this PR re-export internal implementation details that callers outside the module have no legitimate reason to reference? (4) Do any public function signatures expose concrete implementation types (classes, internal unions, internal enums) where a narrower public interface or type alias would be sufficient? (5) Does the PR widen any existing visibility modifier (private -> protected, internal -> exported, etc.)? If yes: is the widening the minimal correct fix, or does it paper over a structural problem -- a new caller that should have been placed inside the module, a missing public method, or a missing seam (interface, event, callback) that would have preserved encapsulation? NOTE -- mandate boundary: this family checks whether existing module boundaries and abstractions are enforced by visibility modifiers, not whether those boundaries are in the right place. If a seam itself appears misdesigned, flag it for patterns_architecture, not here. Start from the module's evident intent, then ask whether the modifiers honour it.",
            "deliverableName": "docs/design/reviewer-findings-visibility-scope.md"
          },
          "modelTier": "mid"
        }
      ],
      "requireConfirmation": false
    },
    {
      "id": "phase-3b-hidden-reflection-scoring",
      "title": "Phase 3b: Hidden Reflection Scoring",
      "runCondition": {
        "var": "needsReviewerBundle",
        "equals": true
      },
      "prompt": "Score and filter the raw reviewer-family findings before synthesis. This pass is internal only -- scores and suppression rationale are never shown to reviewers or included in the final output.\n\nFirst, read the findings from all files generated by the active parallel reviewer families under `docs/design/reviewer-findings-*.md` and compile them into a unified list of raw findings in your context under the key `rawFamilyFindings`.\n\nThen, for each finding in `rawFamilyFindings`:\n\n1. Score it 0-10 on confidence x actionability:\n   - Confidence (0-5): How certain are you that the issue is real and caused by this PR? 0 = speculation, 5 = directly observed in changed lines with clear causal path.\n   - Actionability (0-5): How clearly does the finding tell the reviewer what to do? 0 = vague flag, 5 = specific file+line+remediation step.\n   - Score = Confidence + Actionability (max 10)\n\n2. Apply the finding contract check:\n   - Does it have a specific file and line? If not, -3.\n   - Does it articulate why this PR introduced or worsened the issue? If not, -3.\n   - Does it have a concrete remediation step? If not, -2.\n\n3. Keep findings scoring 5 or above. Convert findings scoring 3-4 to clarification questions. Suppress findings scoring 0-2.\n\n4. For Critical findings: the threshold is 4 (lower bar -- don't suppress potential critical issues).\n\nSet these context keys:\n- `scoredFindings`: array of `{ finding, score, disposition: 'keep'|'clarify'|'suppress' }`\n- `reflectionScoredFindingsCount`: total findings scored\n- `filteredFindingsCount`: findings suppressed (score < 5 for non-critical, score < 4 for critical)\n- `clarificationQuestionsCount`: findings converted to clarification questions\n- `postReflectionCriticalCount`: critical findings remaining after scoring\n- `postReflectionMajorCount`: major findings remaining after scoring\n\nRules:\n- never include scores, thresholds, or suppression rationale in any reviewer-facing output\n- the purpose of this step is to improve signal-to-noise, not to reduce finding count for its own sake\n- a finding that fails the contract check but is clearly important should be rewritten to satisfy the contract rather than suppressed",
      "requireConfirmation": false
    },
    {
      "id": "phase-4-contradiction-and-synthesis-loop",
      "type": "loop",
      "title": "Phase 4: Contradiction Resolution and Synthesis Loop",
      "loop": {
        "type": "while",
        "conditionSource": {
          "kind": "artifact_contract",
          "contractRef": "wr.contracts.loop_control",
          "loopId": "mr_review_synthesis_loop"
        },
        "maxIterations": 4
      },
      "body": [
        {
          "id": "phase-4a-targeted-follow-up",
          "title": "Targeted Follow-Up Bundle",
          "promptBlocks": {
            "goal": "If contradictions, blind spots, or important coverage gaps remain, run only the smallest targeted follow-up needed.",
            "constraints": [
              [
                {
                  "kind": "ref",
                  "refId": "wr.refs.parallelize_cognition_serialize_synthesis"
                }
              ],
              "Prefer one compact targeted bundle over repeated broad delegation moments.",
              "Do not regather broad context unless a contradiction proves the original fact packet is insufficient.",
              "Targeted follow-up output is evidence only and must still be synthesized by the main agent."
            ],
            "procedure": [
              "Before delegating, state the current canonical recommendation direction, the strongest unresolved concern, and what result would change your mind.",
              "If `contradictionCount > 0`, run targeted challenge or validation aimed at the specific disagreement.",
              "If `coverageUncertainCount > 0` or `blindSpotCount > 0`, run the smallest reviewer-family or context follow-up needed to close the gap.",
              "If `needsSimulation = true`, include `routine-execution-simulation`.",
              "If `falsePositiveRiskCount > 0`, include `routine-hypothesis-challenge`.",
              "If `callerEnumerationHighRiskCount > 0` and transitive caller impact remains uncertain, spawn a targeted correctness check focused only on the high-risk callers.",
              "If no trigger fires, do not delegate this step."
            ],
            "verify": [
              "Only the smallest targeted bundle needed was run.",
              "No broad context regather happened without an explicit contradiction-driven reason."
            ]
          },
          "requireConfirmation": false
        },
        {
          "id": "phase-4b-canonical-synthesis",
          "title": "Canonical Synthesis and Coverage Update",
          "prompt": "Synthesize all reviewer-family outputs, reflection scoring, and targeted follow-up into one canonical review state. Work from `scoredFindings` (post-reflection), not `rawFamilyFindings`.\n\n**Part A -- Compare against your hypothesis:**\n- revisit `recommendationHypothesis`\n- what did the evidence confirm?\n- what did it challenge?\n- what changed your mind, what held firm, and what do you explicitly reject?\n- did intent misalignment or transitive caller impact change the direction from what you expected?\n\n**Part B -- Synthesis decision table:**\n- if 2+ reviewer families flag the same serious issue with the same severity, treat it as validated\n- if the same issue is flagged with different severities, default to the higher severity unless the lower-severity position includes specific counter-evidence\n- if one family flags an issue and others are silent, investigate it but do not automatically block unless it is clearly critical or security-sensitive\n- if one family says false positive and another says valid issue, require explicit main-agent adjudication in notes before finalization\n- if recommendation spread shows material disagreement, findings override recommendation until reconciled\n- if simulation reveals a new production risk, add a new finding and re-evaluate recommendation confidence\n- if `intentAlignmentStatus = misaligned`, this is a blocking finding regardless of other synthesis results\n\n**Part C -- Coverage ledger rules:**\n- move a domain from `uncertain` to `checked` only when evidence is materially adequate\n- keep a domain `uncertain` if disagreement or missing evidence still materially affects recommendation quality\n- mark `not_applicable` only when the MR genuinely does not engage that dimension\n- clear `contradicted` only when explicitly resolved by evidence or adjudication\n- clear `needs_followup` only when follow-up has actually been completed or the domain is explicitly downgraded as non-material\n- `transitive_impact` domain: mark `checked` only if caller enumeration ran and high-risk callers were assessed; mark `uncertain` if `callerEnumerationConfidence = Low`\n\n**Part D -- Recommendation confidence rules:**\n- `High`: no unresolved material contradictions, no important coverage domains uncertain, false-positive risk not material, evidence strong enough for the current mode\n- `Medium`: one bounded uncertainty remains but recommendation is still directionally justified\n- `Low`: multiple viable interpretations remain, major contradictions unresolved, or important coverage gaps weaken the recommendation\n\nSet context keys: `reviewFindings`, `criticalFindingsCount`, `majorFindingsCount`, `minorFindingsCount`, `nitFindingsCount`, `recommendation`, `recommendationConfidenceBand`, `recommendationDriftDetected`, `coverageLedger`, `coverageUncertainCount`, `docCompletenessConcernCount`\n\nIf `reviewDocPath` exists, keep it aligned for human readability only.",
          "requireConfirmation": false
        },
        {
          "id": "phase-4c-loop-decision",
          "title": "Synthesis Loop Decision",
          "prompt": "Decide whether the synthesis loop should continue.\n\nDecision rules:\n- if `contradictionCount > 0` -> continue\n- else if `coverageUncertainCount > 0` and the uncertainty materially affects the recommendation -> continue\n- else if `falsePositiveRiskCount > 0` -> continue\n- else if `recommendationDriftDetected = true` -> continue\n- else -> stop\n\nOutput exactly:\n```json\n{\n  \"artifacts\": [{\n    \"kind\": \"wr.loop_control\",\n    \"decision\": \"continue\"\n  }]\n}\n```",
          "requireConfirmation": true,
          "outputContract": {
            "contractRef": "wr.contracts.loop_control"
          }
        }
      ]
    },
    {
      "id": "phase-5-final-validation",
      "title": "Phase 5: Final Validation",
      "promptBlocks": {
        "goal": "Stress-test the current recommendation before final handoff.",
        "constraints": [
          [
            {
              "kind": "ref",
              "refId": "wr.refs.adversarial_challenge_rules"
            }
          ],
          [
            {
              "kind": "ref",
              "refId": "wr.refs.synthesis_under_disagreement"
            }
          ],
          "Validation output is evidence to synthesize, not an automatic reopen signal."
        ],
        "procedure": [
          "Run final validation if any of these are true: `criticalSurfaceTouched = true`, `needsSimulation = true`, `falsePositiveRiskCount > 0`, `coverageUncertainCount > 0`, `docCompletenessConcernCount > 0`, `callerEnumerationHighRiskCount > 0`, or `recommendationConfidenceBand != High`.",
          "Before delegating, state: what is your current recommendation, where are you least confident, and what finding would most likely change your mind now?",
          "Mode-adaptive validation: QUICK = self-validate and optionally spawn ONE WorkRail Executor running `routine-hypothesis-challenge` if a serious uncertainty remains; STANDARD = if validation is required, spawn TWO WorkRail Executors SIMULTANEOUSLY running `routine-hypothesis-challenge` and either `routine-execution-simulation` or `routine-plan-analysis`; THOROUGH = if validation is required, spawn THREE WorkRail Executors SIMULTANEOUSLY running `routine-hypothesis-challenge`, `routine-execution-simulation` when needed, and `routine-plan-analysis`.",
          "After receiving validator output, explicitly synthesize what was confirmed, what was new, what appears weak, and whether your recommendation changed.",
          "Perform a compact confidence assessment: rate each dimension as High / Medium / Low with a one-sentence explanation: `boundaryConfidence`, `intentConfidence`, `evidenceConfidence`, `coverageConfidence`, `consensusConfidence`. Derive final confidence: if boundary is Low -> Low; else if evidence is Low -> Low; else if 2+ dimensions are Medium -> Medium; else if all key dimensions are High -> High. Unresolved disagreement can only lower confidence, never raise it.",
          "Compute `docCompletenessConcernCount`: one concern for each of: missing rationale for any Critical/Major finding, missing ready-to-post MR comment for any Critical/Major finding, recommendation mismatch with canonical findings, still-uncertain/contradicted/needs-followup coverage domains not summarized, any missing required final section.",
          "Set context keys: `intentConfidence`, `evidenceConfidence`, `coverageConfidence`, `consensusConfidence`, `confidenceAssessmentSummary`, `validatorConsensusLevel`, `validationSummary`, `recommendationConfidenceBand`, `docCompletenessConcernCount`."
        ],
        "verify": [
          "If 2+ validators still raise serious concerns, confidence is downgraded and synthesis is reopened.",
          "If exactly one validator raises a concern, it is investigated before deciding whether to reopen anything.",
          "If no validator can materially break the current recommendation and findings are internally consistent, proceed to handoff."
        ]
      },
      "assessmentRefs": [
        "evidence-quality-gate",
        "coverage-completeness-gate",
        "contradiction-resolution-gate",
        "finding-schema-compliance-gate"
      ],
      "assessmentConsequences": [
        {
          "when": {
            "anyEqualsLevel": "low"
          },
          "effect": {
            "kind": "require_followup",
            "guidance": "evidence_quality low: anchor each finding to a specific file/line; remove findings without concrete grounding. coverage_completeness low: investigate uncovered domains or acknowledge gaps in the ledger. contradiction_resolution low: resolve each contradiction or state your position with rationale. finding_schema_compliance low: for every Critical/Major finding, add file+line, causal link to PR changes, and a concrete remediation step."
          }
        }
      ],
      "requireConfirmation": {
        "or": [
          {
            "var": "validatorConsensusLevel",
            "equals": "Low"
          },
          {
            "var": "recommendationConfidenceBand",
            "equals": "Low"
          }
        ]
      }
    },
    {
      "id": "phase-6-final-handoff",
      "title": "Phase 6: Final Handoff",
      "prompt": "Provide the final MR review handoff.\n\nInclude:\n- MR title and purpose\n- intent alignment status and any misalignment finding\n- review mode used\n- final recommendation and confidence band\n- confidence assessment summary, including the most important reason confidence was capped if it was not High\n- counts of Critical / Major / Minor / Nit findings (post-reflection)\n- top findings with rationale -- each must include file, line, causal link to PR changes, and remediation step\n- caller enumeration summary: how many external callers were found, how many were high-risk, and what was assessed\n- strongest remaining areas of uncertainty, if any\n- summary of the coverage ledger, especially any still-uncertain domains\n- ready-to-post MR comments summary\n- any validation outcomes a human reviewer should see\n- review environment status: sources used, sources missing, boundary and context confidence, how limits affected the review\n- path to the full human-facing review artifact (`reviewDocPath`) only if one was created\n\nRules:\n- the final recommendation assists a human reviewer; it does not replace them\n- if `reviewDocPath` exists, treat it as a human-facing companion artifact only\n- be explicit when missing PR/ticket/doc/boundary context limited confidence\n- do not post comments, approve, reject, or merge unless the user explicitly asks\n- if reflection scoring suppressed findings, do NOT mention the count or rationale -- only present the final kept findings\n\nIMPORTANT: Emit a structured verdict via your advance call's output.artifacts[] parameter (complete_step in daemon sessions, continue_workflow in MCP sessions). Required fields on the outer object; enrichment fields are optional on each finding:\n{\n  \"kind\": \"wr.review_verdict\",\n  \"verdict\": \"clean\" | \"minor\" | \"blocking\",\n  \"confidence\": \"high\" | \"medium\" | \"low\",\n  \"findings\": [\n    {\n      \"severity\": \"critical\" | \"major\" | \"minor\" | \"nit\",\n      \"summary\": \"one-line description\",\n      \"findingCategory\": \"correctness\" | \"security\" | \"architecture\" | \"ux\" | \"performance\" | \"testing\" | \"style\",\n      \"file\": \"path/to/file.ts\",\n      \"startLine\": 42,\n      \"endLine\": 47,\n      \"causalLink\": \"one sentence: why this PR introduced or worsened this issue\",\n      \"remediation\": \"one sentence: the smallest credible fix or verification step\"\n    }\n  ],\n  \"summary\": \"one-line overall verdict summary\"\n}\nFor a clean review: findings: []. verdict: clean = no blocking issues, minor = small issues only, blocking = critical or major issues found. file/startLine/endLine/causalLink/remediation are optional enrichment fields -- include them for Critical and Major findings. omit endLine if single-line.",
      "outputContract": {
        "contractRef": "wr.contracts.review_verdict"
      },
      "requireConfirmation": {
        "kind": "human_approval",
        "condition": {
          "not": {
            "var": "recommendation",
            "equals": "clean"
          }
        }
      }
    }
  ]
}
